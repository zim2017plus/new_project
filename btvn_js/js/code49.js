let year = 2022;

if ( year > 0 && year % 400 === 0 || year % 4 === 0 && year % 100 !== 0) {
        console.log("năm nhuận");
        
}
    else {
        console.log("năm không nhuận");
        
    }
